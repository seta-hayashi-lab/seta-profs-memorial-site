# ===========================================
# 瀬田和久先生追悼サイト 環境設定ファイル
# ===========================================
#
# 使用方法：
# 1. このファイルを .env としてコピー
#    cp .env.example .env
# 2. 各項目に実際の値を設定
# 3. .env は .gitignore に含まれているため Git にはコミットされません
#

# -------------------------------------------
# Slack API 設定
# -------------------------------------------

# Slack Bot User OAuth Token
# Slack App の OAuth & Permissions ページで取得
# 形式: xoxb-XXXX-XXXX-XXXX
SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# 投稿先の Slack チャンネル ID
# チャンネル名ではなく ID を使用（例: C01XXXXXXXX）
# チャンネルを右クリック → 「リンクをコピー」で URL 末尾から確認可能
SLACK_CHANNEL_ID=C01XXXXXXXX

# -------------------------------------------
# セキュリティ設定
# -------------------------------------------

# CORS許可オリジン（本番環境では適切なドメインを指定）
# 例: https://example.com
# 開発環境では * でも可
ALLOWED_ORIGIN=*

# -------------------------------------------
# ファイルアップロード設定
# -------------------------------------------

# 最大ファイルサイズ（バイト単位）
# デフォルト: 1GB = 1073741824
# 対応形式: JPEG, PNG, GIF, WebP, MP4, WebM, MOV, AVI
MAX_FILE_SIZE=1073741824

# -------------------------------------------
# 認証設定
# -------------------------------------------

# 管理者パスワード（ハッシュ化前の値）
# 本番環境では強力なパスワードを設定してください
# PHP で password_hash('your-password', PASSWORD_DEFAULT) でハッシュ化して保存推奨
ADMIN_PASSWORD=admin-password-here

# 一般ユーザー用パスワード（追悼メッセージ投稿者向け）
# 関係者に共有するパスワード
USER_PASSWORD=user-password-here

# セッション有効期限（秒）
# デフォルト: 86400（24時間）
SESSION_LIFETIME=86400

# -------------------------------------------
# Git 自動プッシュ設定
# -------------------------------------------

# ギャラリーへの追加/削除時に自動でGitHub にコミット＆プッシュ
# true: 有効 / false: 無効（デフォルト）
GIT_AUTO_PUSH=false

# GitHub Personal Access Token（repo権限が必要）
# https://github.com/settings/tokens で作成
GIT_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx

# GitHubリポジトリ（ユーザー名/リポジトリ名）
GIT_REPO=seta-hayashi-lab/seta-profs-memorial-site
